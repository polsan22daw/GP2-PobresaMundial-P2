import React from 'react';
import {View, Text, StyleSheet, TouchableOpacity} from 'react-native';

const Opcions = () => {
    return (
        <View>
            <Text
                style={{
                fontSize: 30,
                textAlign: 'center',
                marginTop: "20%"
                }}
                > Aqui van las opcions... o no....</Text>
        </View>
    );
};

export default Opcions;